#ifndef DIVISOR_H
#define DIVISOR_H

#include <vector>

class divisorHelper
{
    public:
        static std::vector<unsigned int> getDivisors(unsigned int p_number);
    private:
};

#endif //DIVISOR_H
